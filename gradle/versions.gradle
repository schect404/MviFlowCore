/**
 * HOW TO add new versions to this file:
 *      1. Check whether the version you want to add already exists.
 *      2. Find the section where the version should be added. In case there is no section
 *         already created, create a new one following the current alphabetical order.
 *      3. Following alphabetical order place your new version in the right section
 *         using -Version suffix.
 */

ext {

    /** Android **/
    androidxAppcompatVersion = "1.5.0"
    androidxCoreVersion = "1.8.0"
    androidxLifecycleVersion = "2.5.1"
    androidxNavigationVersion = "2.5.1"

    /** Compose **/
    composeVersion = "1.3.3" // Compose Group: Animation, Compiler, Foundation, Material(3), Runtime, UI
    composeCompilerExtensionVersion = "1.3.0"
    composeCoilVersion = "2.1.0"

    /** Environment **/
    toolsVersion = '7.2.2'
    annotationVersion = '1.1.0'
    jacocoVersion = "0.8.7"
    kotlinVersion = '1.7.10'
    dokkaVersion = '1.4.32'

    /** Google Android **/
    googleAndroidMaterialVersion = "1.6.1"
    googleAppCompatThemeVersion = "0.20.3"

    /** NIL - Native integration layer **/
    nilVersion = "0.2.0"

    /** SDK **/
    androidCompileSdk = 33
    androidTargetSdk = 33
    androidMinSdk = 23

    /** Third party **/
    shimmerLayoutVersion = "1.1"

    /** Testing **/
    espressoCoreVersion = "3.4.0"
    jUnitExtVersion = "1.1.3"
    jUnitVersion = "4.13.2"
}
